generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Product {
  id        String   @id @default(uuid())
  name      String   @unique
  harga     Decimal
  createdAt DateTime @default(now())
}
